"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = {
    functions: [
        {
            name: 'title',
            code: (object, data, embed) => {
                embed.setTitle(data.inside);
                object.content = object.content.replace(data.itself, '');
                return object;
            }
        },
        {
            name: 'description',
            code: (object, data, embed) => {
                embed.setDescription(data.inside);
                object.content = object.content.replace(data.itself, '');
                return object;
            }
        },
        {
            name: 'image',
            code: (object, data, embed) => {
                embed.setImage(data.inside);
                object.content = object.content.replace(data.itself, '');
                return object;
            }
        },
        {
            name: 'thumbnail',
            code: (object, data, embed) => {
                embed.setThumbnail(data.inside);
                object.content = object.content.replace(data.itself, '');
                return object;
            }
        },
        {
            name: 'color',
            code: (object, data, embed) => {
                let color = data.inside;
                embed.setColor(color);
                object.content = object.content.replace(data.itself, '');
                return object;
            }
        },
        {
            name: 'footer',
            code: (object, data, embed) => {
                embed.setFooter({ text: data.splits[0] || '.', iconURL: data.splits[1] || undefined });
                object.content = object.content.replace(data.itself, '');
                return object;
            }
        },
        {
            name: 'author',
            code: (object, data, embed) => {
                embed.setAuthor({ name: data.splits[0] || '.', iconURL: data.splits[1] || undefined });
                object.content = object.content.replace(data.itself, '');
                return object;
            }
        }
    ]
};
